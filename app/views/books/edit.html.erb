<h1><%= i18n_connector('phrases.models.book', 'phrases.actions.update') %></h1>

<%= render 'form', book: @book %>

<%= link_to t('phrases.actions.show'), @book %> |
<%= link_to t('phrases.actions.back'), books_path %>
